const {Router}=require("express");
const router=Router()
const {cartsModel}=require("../models/carts.model");

//traer de la preentrega 1 la lógica y ajustar con persistencia de archivos
router.post("/",(req,res)=>{
    
})








module.exports=router;
